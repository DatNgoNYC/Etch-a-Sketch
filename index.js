/* Instantiate element references */
// we need logic behind the clear all button
// We need logic behind button/input/whatever we'll use to specify number of cells to create
// we need logic to create the cells themselves
const clear = document.querySelector('#clear');
const slider = document.querySelector('#number-of-cells');
const canvas = document.querySelector('#canvas');

